# Node runtime
NODE_ENV=production
LOG_LEVEL=info
LOG_DIR=/home/container/data/log
LOG_NO_COLOR=false

# Database
DATABASE_URL=file:/home/container/data/bot.db

# Auth blob encryption (32-byte hex; generate via: openssl rand -hex 32/node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
AUTH_ENCRYPTION_KEY=replace-with-64-hex-chars

# WhatsApp adapter
WA_ENABLED=true
OWNER_WA=
WA_RATE_MIN_TIME_MS=800

# Telegram adapter
TELE_ENABLED=true
TELEGRAM_BOT_TOKEN=
OWNER_TG=
TELE_RATE_MIN_TIME_MS=50